[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    auth: authentication and identity tests
    spaces: property and spaces lifecycle tests
    iot: telemetry and device monitoring tests
    tasks: maintenance and work items tests
    notifications: system notification tests
